import React from 'react'

function StarfieldSection({containerRef}) {
  return (
    <div ref={containerRef} className='hidden sticky top-0 left-0 aspect-video min-w-full max-w-screen bg-black'>
      
    </div>
  )
}

export default StarfieldSection
